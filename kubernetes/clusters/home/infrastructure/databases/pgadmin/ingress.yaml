apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pgadmin
  namespace: postgres
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`pgadmin.syslabs.dev`)
      kind: Rule
      services:
        - name: pgadmin-pgadmin4
          port: 80
