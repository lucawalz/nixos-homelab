apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storage          # Deploy first
  - networking       # Deploy second
  - rancher          # Deploy after networking (depends on Traefik/cert-manager)
  - databases        # Deploy third
  - monitoring       # Deploy fourth
  - cicd             # Deploy fifth

