apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storage          # Deploy first
  - networking       # Deploy second
  - databases        # Deploy third
  - monitoring       # Deploy fourth
  - rancher          # Deploy fifth
