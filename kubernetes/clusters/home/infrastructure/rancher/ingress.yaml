apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rancher
  namespace: rancher
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`rancher.syslabs.dev`)
      kind: Rule
      services:
        - name: rancher
          port: 80
  tls:
    secretName: rancher-tls