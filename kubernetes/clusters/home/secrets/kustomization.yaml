apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Add encrypted secrets here when ready
resources:
  - cloudflare-tunnel-secret.sops.yaml
# - postgres-passwords.sops.yaml  # Will add after postgres namespace is created
# - grafana-admin.sops.yaml

