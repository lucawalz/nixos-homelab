apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - gotk-components.yaml
  - gotk-sync.yaml
  # - git-auth-secret.yaml  # Uncomment after creating proper encrypted secret
