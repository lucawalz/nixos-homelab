# Common services for all nodes

{ config, pkgs, meta, ... }:

{
  # Example: Tailscale (if using)
  # services.tailscale.enable = true;
  # age.secrets.tailscale-auth = {
  #   file = ../../secrets/tailscale-auth.age;
  #   mode = "0400";
  #   owner = "root";
  #   group = "root";
  # };

  # Placeholder for common services
  # Add services that should run on all nodes here
}

