# Node-level monitoring role (node_exporter, etc.)

{ config, pkgs, meta, ... }:

{
  # Example: Enable node_exporter
  # services.prometheus.exporters.node = {
  #   enable = true;
  #   port = 9100;
  #   openFirewall = true;
  # };

  # Placeholder for future monitoring setup
}

